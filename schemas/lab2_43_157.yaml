# Second Lab — 43.157.213.172:1337
# Status: PENDING — run the scan below and fill in confirmed parameters.
#
# HOW TO FILL THIS IN:
# 1. Run: stelarstrike scan "http://43.157.213.172:1337/" --plugins sqli --verbose
# 2. Paste the full output to Claude and ask:
#    "Extract this as a StelarStrike schema YAML for schemas/lab2_43_157.yaml"
# 3. Replace the PENDING sections below with what Claude extracts.

name: "Lab 2 — 43.157.213.172:1337 (PENDING)"
description: >
  Second Merdeka Siber lab target. Stack and vulnerability details to be
  filled in after initial scan. Update this file by pasting scan output
  to Claude and asking it to extract a schema.
source: "Merdeka Siber Batch 27 — Lab 2"

# PENDING: fill in after running:
# stelarstrike scan "http://43.157.213.172:1337/" --verbose
fingerprints: []  # Replace with actual fingerprints after scanning

stack:
  language: "unknown"     # fill in after scanning
  framework: "unknown"
  db: "unknown"

# PENDING: fill in once SQLi is confirmed
injections: []

endpoints:
  - path: "/"
    method: "GET"
    auth_required: false

additional_findings: []
